# Exclude most debug API routes, but keep the db-ping check
api/debug/**
!api/debug/db-ping.js
!api/debug/dns-check.js

# Exclude duplicate dynamic match routes; we use flat endpoints instead
api/matches/[id].js
api/matches/[id]/
api/matches/ping.js

# Local dev helpers
serve.cmd
serve.ps1
server.js
